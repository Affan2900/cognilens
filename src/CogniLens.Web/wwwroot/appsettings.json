{
  "_comment": "Production API host isn't provisioned yet (Phase 3 infra covers the API's ACA app, not the Web frontend's hosting). Replace before ever publishing this build.",
  "ApiBaseUrl": "https://REPLACE_WITH_API_URL"
}
