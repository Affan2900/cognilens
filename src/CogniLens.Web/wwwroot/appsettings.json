{
  "_comment": "Placeholder by design, not an oversight. This file ships inside the WASM bundle and is a public download, so it must never hold anything secret -- the API key is entered at runtime and kept in localStorage (see Services/ApiKeyStore.cs), never baked in here. cd.yml rewrites ApiBaseUrl with the deployed Api URL before `dotnet publish`, and Program.cs refuses to start if this placeholder survives.",
  "ApiBaseUrl": "https://REPLACE_WITH_API_URL"
}
